VITE_KAKAO_MAP_KEY=your_kakao_map_key_here

# 백엔드 API 베이스 경로 (기본값: '' → vite dev 프록시가 /api 를 게이트웨이로 포워딩)
# 배포 시에는 게이트웨이 주소를 넣는다. 예) https://api.pinmoa.com
VITE_API_BASE=

# (개발 전용) vite 프록시가 포워딩할 게이트웨이 주소. 기본 http://localhost:8080
VITE_GATEWAY_URL=http://localhost:8080
